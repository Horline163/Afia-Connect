const express = require('express');
const router = express.Router();
const mchController = require('../controllers/mchController');

// Define MCH routes here
// Example: router.get('/', mchController.getAllMCHRecords);

module.exports = router;